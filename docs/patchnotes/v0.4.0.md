# v0.4.0 — Logistik-Reservierungen & Karten-Politur

Stabilisierung des Logistik- und Reservierungs-Systems sowie weitere Verfeinerungen am Drag-Controller und am Save-Manager.

## Neu
- Reservierungs-Buchhaltung in `InventoryContainer` und `InventorySystem`
- Neue Tests:
  - `reservation_lockup_test` (Integration) — verhindert Deadlocks bei mehreren Trägern
  - `input_reservation_test` (Unit)
  - `reservation_test` (Unit)

## Verbessert
- `LogisticsSystem`: hartere Garantien im Reservierungs-Lifecycle
- `map_root` Drag-Controller, `resource_badge_layer` und `building_indicator_layer` poliert
- `WorldGrid`, `OverworldSystem`, `PlayerCharacter` und `CameraController` mit kleineren Korrekturen
- `WorldSaveManager` speichert/lädt den neuen State korrekt

## Fixes
- Carrier-Waiting-Test angepasst auf das neue Reservierungs-Verhalten
